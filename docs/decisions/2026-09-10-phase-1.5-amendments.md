# Decision record — Phase 1.5 amendments and direction, 2026-09-10

Recorded verbatim by the ARO dev agent at the architect's instruction ("Commit this to the repo decision record verbatim"). The acts are the architect's. ARO v0.5.2 §16 makes the Phase-1.5 outcome mapping binding once stated; §18 says the architect's instruments are recorded in the repo decision record. This is that record. Nothing below is paraphrased; the agent's own framing is confined to this paragraph and the one note at the end.

---

> Phase 1.5 direction. Amendments signed; go.
>
> AMENDMENT 1 — source substitution. SIGNED.
>
> The Phase-1.5 target is substituted: RLA → the SRS 3-module slice (Documents/SPEC.md @ your recorded digest; C-S8, findings-store, author-agent). The outcome mapping applies unchanged to the substituted target. Recorded before any arm's measurement. — Architect, September 10, 2026
>
> Commit this to the repo decision record verbatim.
>
> AMENDMENT 2 — graph authorship. SIGNED — you are accepted as the slice's graph author, under three conditions:
>
> (i) I perform 100% span review of the slice's L2 graph. (ii) All L3 values are conferred by me from your labeled candidates; plan-derived knowledge may appear as rationale citation, never grounding; the author-agent path conflict (spec .claude/agents/author vs. plan src/author-agent.ts) is presented to me as an adjudication, not a candidate. (iii) This graph is non-graduating: the Phase-4 reference graph gets a fresh, held-out author. — Architect, September 10, 2026
>
> Dispositions on your findings (F-13 to F-17):
>
> FX-SEAM-B — you've confirmed the spec, not found a blocker. B is the joint fixture this repository cannot turn green by design; its green is the INT-2 composition milestone. Re-author it on the real seam, leave it RED, move on.
>
> The fifteen acts — this is the spike working, not failing; spike 2 exists to measure exactly this number. Two instructions: bundle the closures into Specification Records — the store interface as one record, module boundaries, the read API, the layout convention, and the path adjudication as its own item — because I ratify records, not assertions. And spike 2 reports both numbers: assertions ratified and acts performed. Bundling is the granularity lever; I want it measured, not assumed.
>
> Seam-not-in-source — your finding stands and is the most valuable output of the spike so far: a live specimen of the misreading class, including one invention that contradicts the spec. The thesis holds in its true form — one ratified place, projected identically — and for this seam class the ratified place is my Design Graph. Carry this finding prominently in the report.
>
> Finding[ truncation — not yours. The bug (plan signatures rendered into briefs as binding, unvalidated) routes to IA DEV's ordinary queue; your report logs it as an external routing, nothing more. The per-run capture of it stays in the Arm 0 list.
>
> Arm 0 capture list — adopted as you specified (architecture exports for the store, brief interfaces block, bar expected names, built author-agent imports, per run). It goes to OPS, who own Arm 0 and the treatment builds. Hand your projections to OPS when ready — the builds are theirs, as you said.
>
> Go, in your proposed order: fixtures on the real seam, RED · register the SRS · span-cited L2 for C-S8, Finding, Disposition, author-agent, S-S4 · closure candidates queued as bundled records · deterministic projection, re-projection checked · hand off to OPS. Time spent waiting on my acts is data — log the starvation, don't route around it.

---

**Note (agent).** "Your recorded digest" in Amendment 1 refers to the digest recorded in `spike/graph/source-register.json` for `C:/Users/aaron/OneDrive/Documents/SPEC.md`: `sha256:2428b115bafe685044b88f97ac11f6ba28e1179c45989ee1b596b72ae3c89450`. The findings F-13 to F-17 the dispositions refer to are in `spike/report.md`.
